import { HighlightPercentagePipe } from './highlight-percentage.pipe';

describe('HighlightPercentagePipe', () => {
  it('create an instance', () => {
    const pipe = new HighlightPercentagePipe();
    expect(pipe).toBeTruthy();
  });
});
